profiles:
  controllino_micro:
    fqbn: controllino_rp2:rp2040:controllino_micro
    platforms:
      - platform: controllino_rp2:rp2040 (2.0.1)
        platform_index_url: https://github.com/CONTROLLINO-PLC/controllino_rp2/releases/download/global/package_controllino_rp2_index.json
    libraries:
      - ODriveArduino (0.10.9)

# default_profile: controllino_micro # this breaks arduino-nix???

# without arduio-nix, just use arduino-cli compile --profile controllino_micro
# with arduio-nix, just use arduino-cli compile --fqbn controllino_rp2:rp2040:controllino_micro
